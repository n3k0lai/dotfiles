This repository powers a hyperland UI that runs on a NixOS base.

When adjusting any styling, refer to the waves files in .local/share/assets/ for color inspiration.

when given the option, always prefer fish over bash.