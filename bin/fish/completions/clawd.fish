complete -c clawd -f
complete -c clawd -a "status" -d "Show gateway status"
complete -c clawd -a "logs" -d "Follow journal logs"
complete -c clawd -a "restart" -d "Safe restart via systemd"
complete -c clawd -a "fix" -d "Kill rogues + clean restart"
complete -c clawd -a "help" -d "Show help"
